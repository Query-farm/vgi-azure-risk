# vgi-azure-risk

A VGI worker (Microsoft Graph / Entra / Azure) built on
[`@vgi-azure/graph-core`](../vgi-azure-graph-core). Attach from DuckDB via the `vgi`
extension and query `azure.*` table functions. Spec:
`vgi-tasker/vgi-azure-risk-SPEC.md`.

    bun install && bun test
